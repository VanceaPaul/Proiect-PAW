spring:
  datasource:
    url: jdbc:mysql://localhost:3306/quiz_app?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: Darksido1
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    open-in-view: false
  jackson:
    serialization:
      WRITE_DATES_AS_TIMESTAMPS: false

app:
  jwt:
    secret: change-me-please-change-me-please-change-me
    expirationMinutes: 240

logging:
  level:
    org.springframework.security: INFO
